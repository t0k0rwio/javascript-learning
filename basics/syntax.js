// JS Syntax - Rules of writing JavaScript

// 3 ways to declare variables
var oldWay = "var is old";
let modern = "let is modern";
const fixed = "const never changes";

console.log(oldWay);
console.log(modern);
console.log(fixed);

// fixed = "try to change"; // try removing // and run - see the error!

// Naming rules
let firstName = "Ram";     // camelCase - recommended
let age = 25;
let isStudent = true;

console.log(firstName, age, isStudent);

// Single line comment  (like these!)

/*
  Multi line comment
  nothing here runs
*/

console.log("Syntax done!");